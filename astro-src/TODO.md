# Project TODOs

- [ ] Revisit `leadingDash` feature in renderEntry.ts — currently unused (all dashes removed during diff review). Consider removing the feature entirely if no entries need it.
- [ ] Revisit `noCloseParen` feature in renderEntry.ts — likely a mistake in original HTML, not intentional. Consider removing the feature and fixing any entries that use it.
